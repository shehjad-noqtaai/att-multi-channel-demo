NEXT_PUBLIC_SANITY_PROJECT_ID=z6s0fz61
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-10-01
NEXT_PUBLIC_STOREFRONT_BASE_URL=http://localhost:3000

# Server-only Sanity read token (Editor or Viewer). Required when the dataset
# is not publicly readable — without it the gallery stays empty after promote.
SANITY_API_READ_TOKEN=
